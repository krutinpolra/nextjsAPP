// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['mainfacts.com', 'flagcdn.com', 'upload.wikimedia.org'], // Add other domains you use for images
  },
};

export default nextConfig;
